<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.url_field :url %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :important %><br>
    <%= f.check_box :important %>
  </div>
  <div class="field">
    <%= f.label :list_id %><br>
    <%= f.select :list_id, options_from_collection_for_select(@lists, :id, :name, selected: @item.list_id) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
